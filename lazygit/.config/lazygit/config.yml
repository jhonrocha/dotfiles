git:
  commitPrefixes:
    api2: # This is repository folder name
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
    web:
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
    method-integration-impls:
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
    method-integration-api:
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
    method-chrome-extension-server:
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
