git:
  commitPrefixes:
    api2: # This is repository folder name
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
    web: # This is repository folder name
      pattern: "^\\w+\\/(\\w+).*"
      replace: '[$1] '
